# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.11.2] - 2026-09-06

### Changed

- **队长总结卡去掉「交叉核验」卡**（用户反馈：没必要，只要原汁原味的队长回答）。卡片现在只有队长头像、标题、
  复制 / 分享 / 跳转，然后就是总结正文；"总结好像没有正文"的兜底提示保留。
- 追问时的上下文不变、在此写明：「观点讨论」给每家的是**其他几家的最新回答**（不含队长总结），「独立迭代」只发用户写的那句话；
  队长自己的网页对话里天然留着它刚写的总结（同一条对话里继续），其他几家看不到。

### Verification

- JVM 单测 126/126、`lintRelease` 无错误；QA 模拟器上总结卡只剩头像、标题、复制 / 分享 / 跳转豆包网页和正文；
  release 包 API 26 全新安装 `restart_check.py` 12/12。

## [0.11.1] - 2026-09-06

用户在自己手机（窄屏 + 大字号）上试用 0.11.0 后的三条反馈：chip / 动作 / 底部格子被省略号截断；底部这一行要能跳转到各家网页；
队长总结要有队长头像和跳转；豆包把总结写成了"文档"卡片，App 读不到正文，看上去像没总结。

### Fixed

- **窄屏 / 大字号下的截断**：「继续追问」的三个要求 chip 改为按内容定宽、放不下就换行（FlowRow），不再"说得再…"；
  回答卡的动作行（展开全文 / 复制 / 分享 / 跳转网页）同样改为换行，不再把「跳转网页」裁成"跳转"；
  底部格子按实际可用宽度定字号，放不下再自动缩字号（TextAutoSize），永远不省略——"DeepSeek" 不再变成 "DeepS…"；
  选队长的头像格同理。系统字号 1.3 倍实测通过。
- **豆包把总结写进文档卡片**：总结 / 观点讨论的 prompt 明说"直接写在这条对话里，不要生成文档、文件、附件或表格，写完就结束，
  不要反问"。另外加一层兜底（`SummarySanityPolicy`）：总结完成但正文很短、或不长且带"需要我 / 文档 / 已生成"这类话时，
  卡片上明说"总结好像没有正文"，给「跳转网页」看原文和「重新总结」，并且不再显示会被那句话误导的交叉核验卡。
  App 目前读不到文档卡片里的正文（它不在对话 DOM 里）。

### Added

- **底部格子能跳网页**：点未选中的格子是切换查看；已选中的那家格子上写着「点开网页」，再点一下就进它的原网页
  （回答进行中仍显示进度词）。
- **队长总结卡**：标题旁放队长头像；动作行多一个「跳转 X 网页」，随时能去看它的原文。

### Verification

- JVM 单测 126/126（新增 `SummarySanityPolicyTest`，含用户截图里豆包那句原话）；`lintRelease` 无错误。
- QA 模拟器（四家登录态）默认字号：底部格子 desc / 「点开网页」、再点一次跳到 DeepSeek 网页并返回、总结卡头像 + 「跳转豆包网页」、
  三个 chip 与动作行完整，全部通过；系统字号 1.3 倍：格子标签 "DeepSeek" 不省略、动作行四项完整、chip 三条完整、总结卡跳转完整。
  （1.3 倍首次冷启动时弹过一次 "isn't responding"：主线程停在 `HardwareRenderer.setStopped` 等渲染线程，当时主机正在跑 release 打包、
  CPU 压力 64%，主机空闲后重跑无复现，是环境问题不是布局死循环。）
- 豆包当队长的标准总结在模拟器上 22 s 完成、正文写在对话里（"文档卡片"的情况没有复现，prompt 与兜底按用户截图的原话做）。
- release 包在 API 26（minSdk）模拟器全新安装：`restart_check.py` 12/12。

## [0.11.0] - 2026-09-06

用户家人试用后的第三批反馈：三家回答纵向串着排要滚很久；"队长总结"内容浅；想知道每家 AI 现在是什么模式；
想记下每个问题的提问时间；千问弹安全验证时不知道该干什么。布局方案在四套 HTML mock 里由用户选定（C：总览卡 + 底部分段）。

### Added

- **结果页改成"总览卡 + 底部分段切换"**：顶部总览卡每家一行（头像、名字、模式小字、回答第一句预览、状态胶囊），
  最后一行是「队长总结」；底部分段控件每家一格 + 「总结」一格，格里写名字和状态词。点任一行 / 格只显示那一家的完整回答，
  「继续追问」面板跟在那家回答下面。新一轮开始自动切到第一家，打开带总结的历史直接落在「总结」。
- **队长总结（选队长 + 选深度）**：初始回答与观点讨论各家平等，不再有队长徽章 / 置顶 / 队长版讨论 prompt。
  「总结」Tab 里选队长（成员中任一家，默认上次选的 / 第一位）和深度：简明（≤200 字）/ 标准（默认，≤500 字：结论、共识、分歧、建议）/
  深入（≤1200 字：先逐条核对数字与事实，再给结论、依据与风险、分步做法）。喂给队长的是几家的**完整回答**（上限 14,000 字，
  只在超出该站上下文预算时压缩），不再是 2,000 字片段——这是旧总结"浅"的一半原因。选择会记住（`arena_captain`）。
  选「深入」且明确读到队长的深度思考关着时，提醒先去它的网页打开（读不到不提醒）。总结存下深度（`summary.depth`）。
- **模型 / 思考模式小字**：每家网页加载完（登录探测通过后）和每次读回答时，用一段只读脚本抓当前状态，写在总览卡和回答卡：
  DeepSeek 模型（Instant / Expert / Vision）+ 深度思考 / 联网开关（`.ds-toggle-button` aria-pressed，localStorage 兜底）；
  Kimi `.current-model .name` + `.current-effort`；千问顶栏模型名 + 输入区胶囊（快速 / 思考）；元宝 `Switch model` 按钮文字；
  豆包输入区模式名（手机版网页没有深度思考开关）；智谱从 Vuex 读 `commonModel`（GLM-5.2）和 `is_networking`。
  读不到写"模式 未知"，绝不猜。另外回答里出现过有内容的思考块就打「已深度思考」标签（真机实测：Kimi Instant 模式下也会思考，
  所以不能拿模式名推断"关"）。
- **安全验证引导**：卡片直接出「跳转网页去验证」+「重新提取」的提示，说明做完会自动继续。
- **提问时间**：每个问题记录提问时刻，结果页问题卡显示"今天 00:40 提问 · 4 家"，历史列表每条改为提问时间
  （老会话从 id 里的时间戳推回来）。
- 每家回答可「分享」（以前只能复制）。

### Changed

- 设置页去掉「队长」分组（队长改在结果页的「总结」里选）。「讨论总结」统一改叫「队长总结」。
- 总结完成时存的是严格抓取的正式回答（`settledText`），和各家回答一致，不再可能把思考过程当总结存下来。
- 单家回答默认折叠行数从 6 行放宽到 12 行（一屏只放一家）。
- 交叉核验卡：总结里写"几家一致"也算已提炼共识（「深入」档按 prompt 就是这么写的，真机上曾被误标"未标出共识"）；
  「简明」档本来不要求共识 / 分歧两段，不再拿这两个标签挑它的毛病。

### Verification

全部在用户亲自登录六家 AI 的 QA 模拟器（API 34）上跑真实流程（脚本在 `artifacts/qa-harness-20260831/`，不入库）：

- JVM 单测 121/121（新增 `AiModePolicyTest`、队长总结三档 prompt、会话 JSON 的深度 / 模式字段）；`lintRelease` 无错误。
- 四家（DeepSeek / 豆包 / Kimi / 智谱）真实一轮 48 s 全部完成；总览卡小字实读："Instant · 深度思考 关 · 联网 开" /
  "对话" / "Instant · High"（Kimi 同时打了「已深度思考」——Instant 模式下回答里真有 328 字思考块）/ "GLM-5.2 · 联网 关"。
- `real_summary.py`：总览卡 + 底部 Tab 切换只显示那一家；默认队长 DeepSeek「标准」总结 11～55 s；换队长 Kimi +「深入」总结 66 s，
  CDP 读到队长收到的 prompt 首行分别是"做一份标准总结 / 深入总结"；冷启动后落在「总结」Tab，队长与深度记忆保留（23/25，
  两项是脚本没滚屏，截图核对通过）。
- `real_flow.py`：平等观点讨论轮 4/4（29 s，无队长徽章，各站对话地址不变）→ 新问题 → 历史恢复三站地址一致（12/13，一项是脚本没滚屏）。
- `ui_tour.py` 27 项：25 通过，1 项 N/A（六家全已登录，没有"未登录成员"可演示），1 项脚本没滚屏（停掉后切到没成功那格再往下滚，
  「怎么办？」白话说明在，已手动核对）。
- release 包在 API 26（minSdk）模拟器全新安装：`restart_check.py` 12/12（安装、进设置、「重启应用」换 pid、无崩溃、中转进程退出）。


## [0.10.0] - 2026-09-06

用户用 0.9.0 真实使用后的第二批反馈：思考过程被当成回答、出错按钮看不懂、朗读没反应、
「观点讨论」前可以先提要求但没人知道。另外补上另外三家（千问 / 元宝 / 智谱）的真实登录测试。

### Fixed

- **DeepSeek 开着深度思考时，存下来的是思考过程，真正的回答一个字没有**。新版页面里思考内容也是
  `.ds-markdown`（包在 `.ds-think-content` 里），正式回答带 `.ds-assistant-message-main-content`；
  原来取行内第一个 `.ds-markdown`，永远命中思考块。
- **回答提取分成两档**：流式期间宽松抓（混进思考过程也无妨，只是让人看到 AI 在动）；站点给出
  "已结束"的明确信号（消息下方出现复制 / 重新生成那排按钮）之后，用严格规则再提取一次正式回答，
  稳定两次才算完成，存下的也是它（`ResponseSnapshot.finalText` / `settledText`）。
  豆包 / Kimi 同样只认不在思考容器里的正式回答块，多块按顺序拼，"先答一段再思考再答"不会只剩半截。
- **豆包答到一半停顿时被当成"已完成"**（真机实测：存下来的只有"老年人"三个字）。豆包在生成期间就把消息
  操作栏渲染在 DOM 里，只是高度 0、没有按钮；原来只看"存在与否"。现在要求操作栏可见且有按钮才算结束，
  DeepSeek / Kimi 的操作栏判断也一并加了可见性。只有"停止按钮"这种弱信号的站点（千问无网络记录时、智谱）
  多等两轮（共 6 秒）再判完成（`ResponseSnapshot.weakDoneSignal`）。
- **元宝 / 智谱首次真实测试**（用户 2026-09-05 登录）：四家一轮 36 秒全部完成。元宝一条回答按内部小块拼接
  会把加粗片段拆成独立段落（235 字拆成 7 段），改为整块转换并在副本上摘掉深度搜索 / 思考过程 / 来源块；
  结束信号用"Stop Answering 控件消失 + 该条回答的 toolbar 可见"（生成过程采样验证）。智谱认 `.answer .answer-content`，
  `.interact` 操作区可见即结束。
- 出错时的按钮挤在一行按权重分宽度，手机上「重新提取」被裁成"重新"。改成主动作独占一行、其余另起一行。
- 「网络没有连上」提示、断网恢复（见 0.9.0）之外，「重发」遇到错误页先重载。

### Changed

- 「原网页」改叫「跳转网页」（卡片右下角、出错时的动作、等待过久的提示都统一了）。
- **去掉朗读功能**（点了没反应，且不常用）：卡片、总结卡、设置页的朗读入口和整套 TTS 代码一并删除。
- 「继续追问」面板：第一次到这里会提示"可以先提要求，再让它们讨论"（点「知道了」或自己写过就不再出现）；
  输入框留白改成"你的要求（选填），例如：说得再简单些 / 重点比较优缺点"；下面给三个现成要求，点一下就填进去。

### Verification

全部在用户亲自登录六家 AI 的 QA 模拟器上跑真实流程（脚本在 `artifacts/qa-harness-20260831/`，不入库）：

- DeepSeek 开着 DeepThink 问"为什么冬天白天短"：思考块与正式回答分离，存下的是正式回答（远程调试逐项核对）。
- 豆包长回答（五点保暖）：493 字完整落库；修前同类问题只存下 3 个字。
- DeepSeek + 千问 + 元宝 + 智谱 一轮（北京老年人乘车优惠，元宝走了深度搜索）：100 秒四家全部完成，
  2303 / 1295 / 1276 / 708 字，元宝没有混入过程时间线，智谱没有混入"AI生成"标签；开轮时四张卡片同时进入排队。
- DeepSeek + 豆包 + Kimi 一轮：26 秒三家完成，Kimi 存的是回答而不是它的思考过程。
- 界面巡游 `ui_tour.py` 26/27（唯一未过项是"找一家未登录的成员演示"，六家全登录后不适用）、
  队长回归 `captain_check.py` 13/13、JVM 单测 116/116（朗读相关单测随功能一起删除）。
- release 包在 API 26 模拟器全新安装：启动、提问页、设置页、「重启应用」正常。
## [0.9.0] - 2026-09-05

用户登录三家 AI 之后做的第一批**真实**测试（此前所有验证都没有登录态），修掉真机才暴露的问题，
并落实三条用户反馈：发出问题后所有成员同步有反应、「新问题」放到顶部、每个问题都开新对话。

### Fixed（真机才暴露）

- **Kimi 一直"等待回答"直到超时**。Kimi 新版网页把整条回答（含思考过程）都包在 `.toolcall-rollup` 里，
  抓取脚本按 `[class*=toolcall]` 一刀切，把真正的答案也排掉了，五分钟后报超时。
  现在只排除思考块本身（`.toolcall-container.thinking-container` / `.toolcall-flow` / `.toolcall-content`）。
  在真页面上用远程调试验证：过滤后恰好命中 1 块，就是答案。
- **Kimi 的问题被填了两遍**（用户气泡里原文重复）。Kimi 输入框是 Lexical 编辑器，`execCommand(insertText)`
  异步落 DOM，脚本同步一读是空的就又用 `textContent` 硬塞一份并派发 input 事件。改成 180ms 后再核对，
  真没有再兜底。豆包 / DeepSeek 不受影响（它们走 ProseMirror / Slate 分支）。
- 等待过久的卡片副标题原来写"这次没有回答成功"，其实还在等；改成"等了很久还没有回答"。
- **队长徽章贴错轮次**。徽章和"看第一条就够"原来只看"现在的队长设置 + 这轮是观点讨论"，
  恢复一条旧讨论（当时没开队长模式）也会把队长标在一个并没有整合过的回答上。
  现在每一轮记下实际整合的队长（`RoundRecord.captain`，快照多一个可选字段 `currentRoundCaptain`），
  只有这一轮确实由它整合才显示。

### Added（用户反馈）

- **「已排队」状态**：网页池同一时刻只能驱动一个页面，所谓并行其实是逐家发送，以前排在后面的成员
  在轮到自己之前没有任何显示，用户以为只有第一家收到了命令。现在点下「开始讨论」的一刻所有成员
  一起进入「已排队」（请求号提前分配），卡片写明"排第 N 位发送"，叙述改为"问题已交给 3 位 AI"。
  串行模式写"等上一家答完再发"。
- **结果页顶部「＋ 新问题」按钮**。正在等回答时点它会先确认"放弃这一轮？"，不会误触丢掉一轮。
- **每个新问题都在各家网页里开新对话**。以前新问题直接发进上一题的对话，AI 带着旧上下文作答。
  现在开轮前让每家网页回到站点根地址（各站根地址即"新对话"），页面还干净时不重复加载；
  三家同时加载、一起等，最多 25 秒后照常发送。
- **历史会话记录各家的对话地址**（DeepSeek `/a/chat/s/…`、豆包 `/chat/<id>`、Kimi `/chat/<id>`），
  打开历史时把每家网页切回那条对话；冷启动恢复时也切。接着「观点讨论」不会再发进一条空对话里。
  快照多一个可选字段 `conversationUrls`，旧文件照常读。
- 停止等待时还没轮到的成员标为"已停止，这一家还没来得及发送"，「怎么办」直接指向「重发」。
- **"网络没有连上"提示断网时不出现**。`onLost` 里再去问系统"现在有网吗"，系统那一刻往往还把
  刚断掉的网络当成当前网络报回来，答案永远是"有"。现在默认网络一丢直接判无网，恢复时由 onAvailable 置回。
- **断网后的恢复**：网络恢复时自动重载当时没打开的网页；「重发」遇到错误页会先重载再发，
  不再让用户白点一次。

### Verification

全部在用户亲自登录 DeepSeek / 豆包 / Kimi 的 QA 模拟器（API 34）上跑真实流程，脚本在 `artifacts/qa-harness-20260831/`（不入库）：

- 首轮：点「开始讨论」1.5 s 后三张卡片同时有状态（发送中 / 排第 3 位 / 排第 2 位），23 s 三家答完，Kimi 抓到答案、问题只填一遍。
- 队长版观点讨论：DeepSeek 收到"你是这次多 AI 讨论的队长…"，回答含共识 / 分歧，徽章 + "看第一条就够"；总结由队长写。
- 第二题：从三家都停在旧对话的状态开新题，三站地址全换、每站只有 1 条用户消息；从历史打开上一题，三站地址和消息条数回到当时。
- 中途按顶部「新问题」弹出确认框，取消后本轮继续。
- 串行模式 3/3（后两家显示"等上一家答完再发"）；并行中途强杀 → 冷启动"已恢复上次本地讨论"，未完成者"没成功 + 上次 App 被关闭了" → 「重发」补回。
- 断网：提问页 / 结果页出现"网络没有连上"，没网的两家很快"没成功"；联网后提示消失，「停止等待」→「重发」三家全部完成。
- 界面巡游 `ui_tour.py` 与队长回归 `captain_check.py`、JVM 单测 124/124（新增会话文件 captain / conversationUrls 往返测试）。
- release 包在 API 26 模拟器全新安装：启动、提问页、设置页、「重启应用」正常。
## [0.8.0] - 2026-09-05

用户反馈：一个人看不过来那么多信息，希望像 AI 圆桌派那样有个「队长」负责总结，
这样只看队长那一条就行。这版把队长模式做进来，并顺手修掉一个影响所有 prompt 的老问题。

### Added

- **队长模式**（默认开，设置页可关）。队长是圆桌里的一个角色，默认第一位成员，可自己指定：
  - **观点讨论**时，队长拿到的是"队长版" prompt：先用要点写清大家的共识和分歧、
    指出可能有错或被遗漏的地方，最后给结论；篇幅放宽到 400 字（普通成员仍是 200 字）。
    并明确要求"用户很可能只看你这一条，要能独立读懂"，禁止写「如上所述」这类依赖上下文的话。
  - **讨论总结**默认也由队长来写（谁整合谁负责到底）。
  - 结果页把队长**置顶**并打「队长」徽章，副标题显示「已整合大家的观点」；
    标题下写明「X 已经把大家的观点整合好了，看第一条就够」。
  - 同一轮里其他成员默认**收得更短**（2 行，原来 6 行），想细看点「展开全文」。
  - 队长这一轮没答上来（没登录 / 失败 / 被跳过）时自动顺延给第一位参与者，
    不会出现"开了队长模式却没人整合"。
  - 关掉开关立刻退回原来的各家平铺，排序和折叠都不变。

思路参考 Chrome 扩展 AI 圆桌派的 `captain-mode.js`（队长固定为第一位成员），
这里做成可指定 + 界面配套，因为手机上"少看几屏"比"多一个角色"更重要。

### Fixed

- **发给 AI 的 prompt 一直带着多余缩进**（老问题，本次被队长模式的单测顺带抓到）。
  两个 prompt builder 用 `"""…""".trimIndent()`，模板缩进 12 空格，但插进去的
  `$others` / `$viewpoints` 是顶格的多行文本 —— `trimIndent()` 取的是**所有行**的公共
  最小缩进，被这些顶格行拉成 0，模板自己那 12 个空格一个都没去掉。四个空格以上的行
  不少模型会当成代码块，等于把问题和要求塞进了 ``` 里。改成手工拼接，新增
  `ArenaPromptShapeTest` 断言任何一行都不带前导空白。

### Verification

- JVM 单测 120/120（新增 `ArenaCaptainTest` 13 条 + `ArenaPromptShapeTest` 4 条）。Lint 0 错误。
- 真机（API 34）队长模式验证 13/13：`artifacts/qa-harness-20260831/captain_check.py` ——
  队长置顶带徽章、副标题「已整合大家的观点」、标题下提示、队友仍可展开、设置页开关与换人、
  换成 Kimi 后结果页跟着重排、关掉后完全恢复原样。
- 界面全量巡游 26/26 无回归（`ui_tour.py`）。
- **过程中真机抓到一个自己写的 bug**：队长解析原本按「已登录家数」判断，启动时 WebView 逐个加载、
  已登录数短暂只有 1 家，队长直接变 null，徽章和排序当场抖。改成只看成员列表，
  并补 `captainIsStableWhileMembersAreStillLoggingIn` 钉死。
## [0.7.0] - 2026-09-05

面向家人（含中老年）的整体改版：界面对标豆包 / Kimi / 微信「我」页的样式，
每个环节都加了引导，出错时给白话说明和一键修复。功能范围不变。

### Changed（界面）

- **「净白」改成无描边的填色容器**。原来是"白底 + 发丝描边卡片"，看起来像线框图；
  现在输入框、示例、列表、回答卡片全部是浅灰色块压在白底上（豆包 / Kimi / ChatGPT 的做法），
  只保留渐变标题这一处签名元素。其它五套皮肤保留描边与阴影，不受影响。
- **底栏改成三个真正的去处：圆桌 / 历史 / 设置**。原来底栏放的是每家 AI 的原网页入口，
  家人点进去看到一个要登录的网站会不知所措。原网页现在从登录引导、结果卡片「原网页」、
  成员页进入，看网页时底栏隐藏，只留一个明显的「返回圆桌」。
- **首页只剩一个永远可点的主按钮**。没登录够两家时按钮写「先登录 AI（还差 N 家）」，点了直接去登录；
  问题空着时点它会把光标放进输入框并提示；不再出现灰掉的大按钮和三处重复的「去连接」。
- **回答方式（同时 / 依次）移到设置页**，首页少一整块；选择会记住。
- **结果页状态只说一次**：原来同一件事有六种表达（标题副标、两个胶囊、"已完成 N 轮"、卡片副标、
  状态胶囊），现在是一句人话（"2 位已回答，还在等豆包…" / "初始回答完成，3 位 AI 都回答了"）。
  进度条只在进行中显示，不再是一条黑色实心条。
- **设置 / 历史 / 成员 / 登录引导改成分组列表**（微信「我」页 / iOS 设置的样式）：
  圆角分组、行高 56dp 以上、右侧箭头，替代一张张独立卡片。
- 正文字号从 16sp 提到 17sp，次要文字 14→15sp；`muted` 与 `accent` 调深到白底对比度 5:1 以上。
  长辈皮肤在此基础上再乘 1.18，正文达到 20sp。

### Added（引导）

- **首次启动三步说明**：登录 AI → 写问题 → 让它们互相核对，一个「开始使用」。
  只出现一次；设置页「使用说明」可再看。
- **登录引导页按步骤编号**：第 1、2、3 家，已登录打勾；下方一组「怎么登录？」三句话说清。
- **网页页顶部登录提示条**：打开一家未登录的 AI 时，顶部写明"在下面的网页里登录 X，登录成功后这里会提示你返回"；
  探测到登录成功后换成绿色「X 已登录」+ 一个「返回圆桌」按钮。从登录引导进入的网页，返回时回到引导页接着下一家。
- **首页成员一行**：谁在圆桌上、几家已登录（绿）或还差几家（橙），整行可点。

### Added（兜底）

- **出错时的「怎么办？」**：每家 AI 失败时，把开发者诊断文案翻译成一句白话原因 + 一句下一步，
  按钮按建议排序、主动作用填色按钮。等待过久（超过阈值）也会提前给出提示。
  讨论总结失败同样有说明和「重新总结」。映射逻辑在 `ArenaErrorHelp`，纯 Kotlin，有单测保证不出现术语。
- **断网提示**：一句"网络没有连上，请打开 Wi-Fi 或手机流量"，联网后自动消失。
  此前六家网页各自报一串 `net::ERR_…`，家人看到的是六个红色失败。
- **上次异常退出的提示**：非弹窗的提示条，"已登录的 AI 都还在"，可「重新开始」或「知道了」。
- **设置页「遇到问题？按顺序试」**：重新加载 AI 网页（登录不丢）→ 清除卡住的讨论（历史与登录保留，需确认）
  → 重启应用（需确认）。「重启」用独立进程中转（ProcessPhoenix 的做法），不依赖会被系统推迟的 AlarmManager。

### Verification

- JVM 单测 99/99（新增 `ArenaGuidanceTest` 11 条：出错说明不得出现术语、每类原因给出主动作、轮次叙述文案）。
- Lint 0 错误（仅 2 条既有告警）。release 包 1.45 MB，签名为 release 证书。
- 真机巡游脚本 `artifacts/qa-harness-20260831/ui_tour.py`（API 34）27/27 通过：首次引导 → 首页 → 断网提示出现与消失
  → 崩溃提示 → 设置（分组、回答方式、修复工具、确认框）→ 外观 → 成员 → 登录引导 → 未登录网页的顶部提示条
  → 从网页返回落回引导页 → 历史 → 结果页（夹具会话：两家成功 + 一家超时 + 总结）→ 「怎么办？」说明 → 长辈皮肤 + 大字。
- 「重启应用」在 debug（API 34）与 release（API 26，minSdk）上各验一次：主进程 pid 变化、约 1 秒回到前台、
  无崩溃日志、中转进程退出（`restart_check.py` 11/11 与 12/12）。
- 结果页截图来自一份按 `ArenaSessionJson` 格式写入的夹具会话（`seed_session.py`）：QA 模拟器上已没有任何 AI 登录态，
  真实一轮跑不起来；夹具只用于验证版式与出错说明逻辑。
## [0.6.2] - 2026-09-04

围绕一个问题：**升级之后登录态不能丢**。登录态存在 WebView 的 Cookie 库里，随 App 数据活着；
只要包名、签名密钥、versionCode 递增三件事不变，覆盖升级就不会碰它。这版把能加的保障都加上。

### Added

- `android:hasFragileUserData="true"`：万一用户（或签名不一致时）不得不卸载，Android 10+ 会弹出
  「是否保留应用数据」——选保留，重装后登录态还在。
- 退后台时 `CookieManager.flush()`：WebView 的 Cookie 是异步落盘的，刚登录完就被系统杀进程，
  登录态可能还没写到磁盘。此前代码里没有任何一处 flush。
- 发布脚本加两道硬闸门（仓库外 `.ecs_android_publish.py`）：APK 的签名证书 SHA-256 必须等于
  release 证书指纹（防止把 debug 签名或换了钥匙的包发出去——那会让所有已装用户被迫卸载重装）；
  versionCode 必须大于线上正在发布的版本（防止发一个装不上的"降级"）。
- 下载页明确写「直接覆盖安装，不要先卸载旧版，卸载会丢登录」。

### Verification

- 真机（API 34）从 code 9 覆盖升级到 code 10：WebView `Cookies` 库 md5 升级前后一致，
  `firstInstallTime` 不变、`lastUpdateTime` 更新 —— 数据目录未被触碰。
- 代码审计：全工程没有任何调用会清 Cookie（`removeAllCookies` / `clearCache` 等均不存在）。
## [0.6.1] - 2026-09-04

真机验证 0.6.0 时发现的两处：

### Fixed

- **看到新版本提示后重启 App，提示就没了**。检查结果原来只放内存，自动检查又是一天一次，
  用户退出再进要等到明天才会再看到。现在成功拿到清单就缓存，启动时先按缓存显示，再按节奏刷新；
  点过「以后」的版本号仍按记录不再在首页打扰。
- 设置页更新卡片的标题和按钮都叫「检查更新」，有歧义（自动化点中了不可点的标题）。标题改为「版本更新」。

### Verification

- JVM 88/88 PASS，Lint 0 条问题。
## [0.6.0] - 2026-09-04

应用内自动检查更新 + 自建站发布渠道。这个 App 走不了应用商店（核心机制是驱动六家网页版，
商店审核过不去），家人靠 APK 直装，于是「怎么知道有新版」是真正的痛点。

### Added

- **应用内更新检查**（`ArenaUpdateChecker`）。启动时最多一天自动查一次，设置页可随时手动查。
  读自建站上的 `https://ai.lt-stockpartner.tech/android/version.json`，只比对 `versionCode`
  （单调递增，不解析版本号字符串）。有新版本时首页出横幅（可点「以后」按版本号记住不再打扰），
  设置页始终显示更新卡片；「安装」用系统浏览器打开 APK 直链，同签名覆盖安装，登录态保留。
  只用 `HttpURLConnection` + `org.json`，不为一个请求引 OkHttp 把包撑大；`apkUrl` 强制 https。
- **自建站下载页** `https://ai.lt-stockpartner.tech/android/`：备案域名 + 国内 ECS，微信里能打开
  （微信内不能直接下 APK，页面上写明「右上角 → 在浏览器打开」）。带二维码、安装步骤、SHA-256。
- 发布脚本 `C:/Users/lintian/.ecs_android_publish.py`（仓库外，与其它 `.ecs_*.py` 同一套约定）：
  `aapt2` 读包内真实 versionCode/versionName、算 sha256、生成 version.json + 下载页，SFTP 到
  stage 目录后原子换名上线，不动 caddy 进程；上线后回读 version.json 与 APK 的 Content-Length 校验。
- JVM 单测加 `org.json:json` 真实现：`android.jar` 里的 org.json 是桩，一调就抛 "not mocked"，
  此前 3 个「应抛异常」的用例其实是靠桩抛的异常假通过。

### Verification

- JVM 88/88 PASS（新增 `ArenaUpdateCheckerTest` 7 项），Lint 0 条问题。
- 发布脚本 dry-run：versionCode/versionName 从包内读出、下载页零外链、二维码内联 SVG。
## [0.5.2] - 2026-09-04

三个真机用出来的问题：答案被截、输入框被压住、历史打不开。

### Fixed

- **答案只抓到结尾一小段**。锚定成功时 `collectText` 还额外要求选择器出现在
  `fragmentSelectors` 白名单里，而元宝和智谱传的是空数组，于是它们**永远只取最后
  一个匹配节点** —— 站点把一条回答拆成多个 markdown 容器时，抓回来的就只有收尾
  那一句。Kimi 的 `finalBlocks.pop()` 是同一个病。
  现在锚定成功（`data-ai-arena-request` 命中本轮那条用户消息）就整段拼接：范围已经
  确定全部排在本轮之后，拼接是安全的。新增 `topLevelOnly` 先剔除互相嵌套的候选，
  避免选择器同时命中容器和它的子段落时把同一段算两遍。
  没有锚点时仍旧退回"取最后一个"，因为那时范围只是猜的。
- **输入框被底部主按钮压住 / 键盘反复弹回**。底部标签栏在键盘弹出时被完全挡住，
  却仍然占着 `Scaffold` 的 `innerPadding.bottom`（约 85dp），而内容侧又叠了一层
  `imePadding()` —— **两份底部内边距叠加**，把中间可滚区挤到只剩一行高，输入框因此
  被固定在底部的「开始讨论」压住，按钮和键盘之间还多出一条 85dp 的空白带。
  现在键盘打开时不摆放底栏；同时输入框压矮一档（minLines 3→2），让分隔线和
  语音/清空那一行也留在可视区内。
- **`windowSoftInputMode` 显式声明为 `adjustResize`**。此前没写，交给系统按布局猜，
  和 `enableEdgeToEdge()` + `imePadding()` 这套组合配起来行为不稳定。
- **历史记录点「继续」总是失败**。原来是 `if (isBusy) return false`，调用方只能笼统
  提示「该历史记录无法恢复」：用户刚问完一轮、讨论总结还在跑的时候点历史，必然撞上
  这条，而且完全看不出原因。现在忙的时候**先停掉本轮再打开**（这正是点「继续」想表达
  的意思），并按结果给出具体文案。

### Added

- `ArenaSessionRepository.forget(id)`：索引里有、文件却读不出来的死条目，点开时
  自动从列表移除，不再每次点每次失败。
- `RestoreOutcome` 枚举，取代原来的 `Boolean`，让界面能说清楚发生了什么：
  已打开 / 已停止进行中的一轮并打开 / 文件已损坏已移除 / 存储不可用。

### Verification

- JVM 81/81 PASS，Lint 0 条问题。
- 抓取范围在**真实 Chrome** 里验证三种形状：三段并列且锚定成功 → 全部拼接；
  未锚定 → 仍只取最后一段；选择器同时命中容器与子段落 → 自动去重不重复。
- 真机（API 34）验证键盘：点开输入框后 `mInputShown` 全程为 true，输入、等待 3 秒、
  再输入都没有回弹；「开始讨论」与键盘之间不再有空白带。
## [0.5.1] - 2026-09-04

答案不再被拍平成无格式文本：抓取侧改为把 DOM 转成 Markdown，渲染侧原生还原结构。

### Added

- **DOM → Markdown 序列化器**（`ArenaMarkdownScript`）。此前全程走 `element.innerText`，
  等于在答案离开网页的那一刻就把结构拍平了。丢掉的东西比看上去多：
  加粗、标题、代码块、表格全没，**有序列表的序号因为是 `::marker` 伪元素干脆不在
  `innerText` 里**，引用角标则退化成混在正文中的裸数字
  （「…（Recursive Self-Improvement）1 4 8 。」）。
- **引用角标转上标**（¹ ⁴ ⁸）。判定刻意不写死某家的类名，用一组通用信号：
  必要条件是「几乎是叶子节点」+「整段文本是 1–3 位纯数字」——正文里的数字是文本节点
  的一部分、不会单独包一层元素，这条本身就滤掉了绝大多数误判；再叠加 `sup` 标签 /
  类名含 cite 词根 / 指向 `#cite` 的链接 / `vertical-align` 是上下标 / 字号明显小于
  父元素 中的任意一条。六家站点通吃，改版也不容易失效。
- **`ArenaMarkdown`**：纯 Kotlin 的块级 + 行内解析器，只依赖标准库，可直接 JVM 单测。
- **`MarkdownText`**：原生渲染标题、有序/无序列表（含嵌套缩进）、加粗/斜体/删除线、
  行内代码、代码块（带语言标签、横向滚动）、表格（横向滚动）、引用和分割线。
  折叠预览按**块**截断而不是按像素裁切，所以不会把某一行从中间切开，
  而且折叠态同样保留格式——它是默认视图，不能退回纯文本。

### Changed

- 每家 AI 的回答和「讨论总结」都改用 `MarkdownText` 渲染。
- **抓取上限 12,000 → 14,000 字**：Markdown 标记本身要占字符（实测约 +8%），
  阈值不跟着抬就等于变相少抓了一截正文。
- 朗读文本清洗增强：代码块整段换成一句「代码块」而不是逐字念，
  引用角标上标字符直接丢弃，表格竖线与分隔行不再被念成一串破折号。

### Notes

- **结构保留，排版统一**：刻意不去还原各家网页自己的样式。六家站点长得互不相同，
  各留各的会让圆桌变成大杂烩，而圆桌的价值恰恰是横向对比。
- 这条链路的输出还会原样喂给「观点讨论」和「讨论总结」的 prompt，
  所以保住结构不只是观感问题，也直接影响后续两步的输入质量。
- 旧会话存的是无格式纯文本，解析后原样按段落渲染，向后兼容。
- 序列化失败时回落 `innerText`，绝不因为转换出错把答案变成空。

### Verification

- JVM 81/81 PASS（此前 64）：新增 `ArenaMarkdownTest` 11 项、`SpeechMarkdownTest` 6 项。
- 序列化器在**真实 Chrome** 里对仿 DeepSeek 答案 DOM 跑通，角标 / 有序列表序号 /
  嵌套列表 / 表格 / 代码块 / 链接 / `3 \* 4` 转义全部正确，按钮与 `aria-hidden` 已跳过。
- Lint 0 条问题。
## [0.5.0] - 2026-09-01

界面换成「净白」：去掉彩色渐变顶栏，顶栏与页面同为纯白，提问页用一句渐变大标题作为视觉重心。

### Added

- **新皮肤「净白」，并设为默认**。顶栏不再是青绿→蓝的色块，而是与页面同色的纯白；
  卡片零阴影、只留一道 1dp 发丝描边；强调色换成紫 `#6E5BFF`。
  参考的是千问手机版的做法（纯白、零卡片、零边框），那个页面本来就在本 App 的 WebView 里。
- **渐变大标题**「今天想问点什么？」，紫→品红填字，是这套皮肤唯一的强视觉元素。
  新增 `ArenaHeading`：皮肤定义了 `headingGradient` 就用渐变填字，否则退回纯色，
  因此其余四套皮肤观感完全不变。
- `headlineLarge` 字阶（30sp）。**中文在系统字体下只有 400 一个字重**，
  所以这里靠字号和渐变拉层次，不靠字重堆叠。

### Changed

- 提问页顶部结构改为：小字品牌「圆桌」+ 渐变大标题 + 一句说明，取代原来的
  「AI 圆桌 / 一个问题，多家 AI 一起回答」两行标题。
- **删掉全工程唯一一个 emoji 图标**（语音按钮上的 🎤），改为纯文字「语音输入」。
  原生 App 里出现 emoji 会立刻显得像网页壳。

### Verification

- JVM 单元测试 64/64 PASS，Lint 0 条问题。
- debug 与 release 均构建通过；release 为已签名包，**APK 1.4 MB**（未因改版变大）。

## [0.4.0] - 2026-08-31

把交付质量补到「可上架」水准：发布链路、包体、图标、崩溃可见性和 minSdk 验证。

### Added

- **release 签名链路**：`signingConfig` 从仓库外、且 gitignore 的 `keystore.properties`
  读取；文件不存在时（例如 CI）照常构建，只产出未签名包。首个正式 release 密钥已生成。
- **自适应启动图标**：此前把一个 24dp 矢量直接当 launcher icon 用，桌面上没有背景、
  没有安全区、也没有 Android 13+ 的主题图标。现在是标准 adaptive-icon
  （品牌色底 + 白色前景 + monochrome），前景内容收在 108 画布的中心安全区内。
- **本地崩溃记录**：崩溃时在本机写一份含版本号、机型、系统版本和完整堆栈的报告，
  设置页可导出分享、可清除。不联网、不上传，与「登录信息只保存在本机」一致。
  已用真实 uncaught exception 验证：报告落盘 → 设置页出现卡片 → 可导出。
- 设置页明确声明本应用不是任何 AI 厂商的官方客户端，自动化操作存在账号风险。

### Changed

- **release 开启 R8 + 资源收缩**：APK 从 8.65 MB 降到 **1.43 MB**（−83%）。
  本工程无反射、无 `@JavascriptInterface`，注入网页的脚本都是字符串常量，默认规则即可。
- **CI 增加 `assembleRelease`**：R8 只在 release 生效，不进 CI 等于没人验证。
- 等待回答超过 75 秒仍一个字都没读到时，文案从单纯报秒数换成可操作提示
  （「可点原网页看看是否需要登录或完成验证」）。此前要干等满 5 分钟才知道出了问题。
- 按路径精确豁免 `ObsoleteSdkInt`：lint 建议把 `mipmap-anydpi-v26` 合并成
  `mipmap-anydpi`，但实测去掉 `-v26` 后资源合并器不收录该目录、AAPT2 直接报错。
  豁免只作用于该目录，其余位置仍然检查。

### Fixed

- `providerHeaderPx` 用 `mutableIntStateOf` 避免装箱（lint AutoboxingStateCreation）。

### Verification

- JVM 64/64 PASS；Android instrumentation 34/34 PASS。
- **Lint 0 条问题**（v0.3.0 时还有 1 Hint；本次新增的 1 Warning 也已处理）。
- debug 与 release 两种构建都通过；release 为已签名包。
- **API 26（minSdk）真机验证**：全新安装 + release→release 覆盖升级，
  `firstInstallTime` 保持不变（登录态可保留），启动、渲染、图标均正常。
  这是 v0.3.0 之前从未跑过的路径。
- 真机压力测试 30 项检查全通过（旋转 / 深浅色 / 系统字号 / 五套皮肤 / 大字叠加 /
  Monkey 1500 / 强杀重启）。
- 崩溃记录用 `am crash` 触发真实 uncaught exception 验证，端到端可用。

## [0.3.0] - 2026-08-31

界面全面重做 + 一批只有在真机上才会暴露的稳定性问题修复。升级为覆盖安装，登录态保留。

### Added

- **五套界面风格，可随时切换**：清朗（默认）、墨韵、夜航（深色）、长辈、暖阳。
  每套风格同时决定配色、圆角、描边粗细、阴影模型和字号基准，不只是换主色。
  入口在「设置 → 界面风格」，选中即时生效并本地保存。
- 「长辈」风格在字号、触摸目标、按钮高度和描边粗细上整体加大，面向中老年用户。
- 「夜航」为真正的深色模式，系统状态栏与导航栏图标跟随反色。
- 提问页新增示例问题、字数计数和一键清空。
- 每家 AI 的回答支持**展开全文**、单独复制和朗读，不再只能看前几行。
- 提问时提前给出上下文预算提醒：问题长到会让「观点讨论」「讨论总结」失败时，
  在输入阶段就说明原因和建议字数，而不是等到点按钮才报错。
- 进行中的一轮显示进度条、轮次信息和「N / M 已回复」。

### Changed

- 主行动按钮固定在底部，屏幕再小、字号再大也不会被推到看不见的位置。
- 结果卡片按状态着色边框（进行中 / 完成 / 失败），状态胶囊在等待时呼吸闪烁。
- 历史页显示全部本地会话（此前只显示前 8 条，但标题写的是全部条数）。
- 成员选择改为整行卡片，标注实验性适配和登录状态。
- 注入网页的 JS 字面量转义换成自实现的 `ArenaJs`，额外转义 U+2028 / U+2029，
  并且可被 JVM 单元测试直接覆盖。

### Fixed

- **网页自动化死锁**：WebView 渲染进程被系统回收时，已投递的 `evaluateJavascript`
  回调会被静默丢弃。此前 `automationService` 会永久非空，之后所有发送和网页显示
  全部失效，只能杀进程重启。现在整条自动化链有 45 秒看门狗，渲染进程退出时立即
  释放，登录探针也有独立超时。
- **主线程落盘导致的卡顿/ANR**：流式回答期间每秒约两次全量 JSON 编码加两次 fsync
  都在主线程。现已移到后台单线程，去抖从 250ms 提到 1200ms。
- **「开始新问题」后冷启动仍恢复旧会话**：落盘异步化引入的顺序问题，
  用落盘代号（generation）和同步收尾修复。
- **配置变更摧毁整轮讨论**：旋转屏幕、系统深浅色切换、系统字号变化、分屏都会重建
  Activity，销毁全部 WebView 并把进行中的回答判为失败。已声明 `configChanges` 自行吸收。
- **会话文件可能整份消失**：`writeAtomically` 先删后改名，进程在中间被杀就没了。
  改为直接 rename 覆盖（POSIX 语义本就是原子替换）。
- **重发失败会丢掉已有的部分回答**：重发前会清空 response，失败后不再回填。
  现在重发拿不到内容时，把上一次的部分回答还回去。
- **单家发送无端到端超时**：并行模式下一家是在上一家 send 回调后才发的，
  任何一次回调丢失都会让整轮永远停在「正在发送」。现在有 60 秒兜底。
- **停止/重置不会中止网页侧自动化**：用户紧接着重发时会出现两条自动化链互相打断。
  `ArenaGateway` 新增 `cancelAutomation()`。
- **轮次进行中改成员会销毁正在收答案的 WebView**：现在成员在忙时不可改，
  且当前参与者的 WebView 受保护不被回收。
- **选择器优先级失效**：`querySelector("a, b, c")` 返回的是"文档中第一个匹配任一
  选择器的元素"，不是"第一个能匹配的选择器"。输入框和发送按钮的候选表改为逐个尝试。
- **千问回答可能被提前判完成并截断**：SSE 已建 record 但还没解析出内容时，
  `streaming` 会停在 false。现在网络答案为空时回落到 DOM 探测。
- **千问抓包缓冲区 O(n²)**：每个分片都重新 split + JSON.parse 最多 2MB 缓冲。
  改为只解析新到达的完整行，缓冲上限降到 200KB。
- 千问 / 元宝 / 智谱的 `data-ai-arena-request` 标记此前算了但从不使用；
  现在真正用它按文档顺序限定答案范围，虚拟列表回收掉标记后再退回计数基线。
- DeepSeek、智谱的 `[class*=stop]` 未限定标签，可能命中任意 class 含 stop 的元素，
  导致 `streaming` 恒为 true、必然走满 300 秒超时。已收紧为 `button[class*=stop]`。
- TTS 引擎不可用时，点朗读会把界面标成「正在播放」却永远没有声音，也回不到可用状态。
- 12,000 字截取可能从代理对（emoji）中间切开，转 UTF-8 后变成问号。
- `destroy()` 之后迟到的 JS 回调仍可能新建 WebView，泄漏已销毁的 Activity。
- `show()` 被 Compose 重组高频重入，形成约 600ms 周期的登录探针自激循环。
- App 退到后台时不再让 3-4 个聊天页继续跑定时器和动画。
- `rememberSaveable` 恢复的枚举名改用容错解析，未来删枚举常量不会崩溃。
- 会话文件加版本闸门，更高版本宁可读不出也不静默丢字段。

### Verification

- JVM 单元测试 64/64 PASS（此前 30）。
- Android instrumentation 34/34 PASS。
- Lint 0 error / 0 warning，Debug APK 构建 PASS。
- 真机（Android 14 模拟器）压力测试：6 次连续旋转、4 次深浅色切换、3 档系统字号变化、
  12 轮页面来回切换、1500 次 Monkey 事件、强杀重启，全部无崩溃无 ANR。
- 5 套皮肤逐一切换并截图核对；长辈皮肤叠加大字模式验证通过。
- 20,000 字与 24,050 字提问的两级提示分别验证。
- 覆盖安装保留首次安装时间与 49,152 字节 Cookie；千问 / 元宝 / 智谱 3/3 仍可用。

## [0.2.0] - 2026-08-31

### Added

- 千问、元宝、智谱网页适配与成员选择。
- 独立迭代、观点讨论和讨论总结。
- 本地历史恢复、单家重发/重新提取/跳过。
- 语音输入、大字模式、回答/总结朗读。
- 总结复制、系统分享和交叉核验提示。
- 上下文预算、长回答截取和最近 8 轮有界历史。
- 首页、历史、设置和成员管理的极简页面结构。

### Changed

- 独立迭代现在要求非空 Prompt，并只发送用户本轮原文。
- 首页移除字数计数、快捷问题、最近讨论和辅助使用大卡片。
- 设置页显示 `versionName` 和 `versionCode`。

### Fixed

- 千问页面已有正确答案但残留 CAPTCHA iframe 时被误判失败。
- 千问“重新提取”重复返回验证码错误。
- 验证码读取错误过早终止；现在允许页面恢复并保留明确超时。
- 豆包发送按钮误命中、幽灵草稿、辩论竞态和图片行提取问题。
- 内部 request ID 曾进入用户可见 Prompt 的问题。

### Verification

- JVM 30/30、Android instrumentation 34/34、Lint 和构建全部通过。
- 完成真实网页并行、串行、独立迭代、观点讨论、总结及辅助功能压力测试。

## [0.1.0] - 2026-08-30

- Android 原生 MVP 初始版本。
- DeepSeek、豆包、Kimi 网页复用登录态。
- 并行/串行回答、基础结果提取与圆桌控制界面。
