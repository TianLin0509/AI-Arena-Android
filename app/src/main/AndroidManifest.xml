<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 只用来判断"有没有网"并给出一句话提示；普通权限，无需用户授权。 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <queries>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiArena"
        android:usesCleartextTraffic="false">
        <!--
            configChanges 是必须的：WebView 池与进行中的圆桌轮次都挂在 Activity 上，
            一旦系统重建 Activity，已登录的页面会全部重载、正在等待的回答会被判为失败。
            旋转、深色模式、系统字号、分屏、折叠屏形态变化都必须由 App 自己吸收。
        -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|uiMode|density|fontScale|keyboard|keyboardHidden|navigation|locale|layoutDirection"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
            「重启应用」用的透明中转页，跑在独立进程里：由它杀掉主进程再拉起启动页。
            见 ArenaRestart.kt。不导出、不进最近任务。
        -->
        <activity
            android:name=".ArenaRestartActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:process=":arena_restart"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>
</manifest>
